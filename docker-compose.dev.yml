version: "3.3"
services:
  curious-frontend-2:
    environment:
      - TZ=Asia/Seoul
    build:
      context: ./
      dockerfile: ./Dockerfile.development
      labels:
        - flask
    volumes:
        - .:/code
    image: "frontend-engineer-test"
    ports:
      - "5055:5000"
    restart: on-failure

